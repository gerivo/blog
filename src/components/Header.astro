---
const { title, withGoBack } = Astro.props;
---

<div class="flex gap-2 items-end">
    {!!withGoBack && <a href={withGoBack} class="text-primary hover:underline text-3xl">; </a>}
    <h1 class="underline text-4xl text-center">{title}</h1>
</div>
